<Window x:Class="RVTuk.UI.Views.IndexProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RVTuk – Indexing Library"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding CurrentFileName}" TextTrimming="CharacterEllipsis"
                   Margin="0,0,0,6" FontStyle="Italic"/>

        <ProgressBar Grid.Row="1" Height="18" Value="{Binding ProgressValue}" Margin="0,0,0,6"/>

        <TextBlock Grid.Row="2" Margin="0,0,0,6">
            <Run Text="Processing family "/>
            <Run Text="{Binding CurrentCount, Mode=OneWay}"/>
            <Run Text=" of "/>
            <Run Text="{Binding TotalCount, Mode=OneWay}"/>
            <Run Text="   "/>
            <Run Text="Elapsed: "/>
            <Run Text="{Binding ElapsedTime, Mode=OneWay}"/>
            <Run Text="   "/>
            <Run Text="Remaining: "/>
            <Run Text="{Binding RemainingTime, Mode=OneWay}"/>
        </TextBlock>

        <Separator Grid.Row="3" Margin="0,4,0,8"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Rebuild Index" Width="100" Margin="0,0,8,0"
                    Command="{Binding RebuildIndexCommand}"/>
            <Button Content="Cancel" Width="70"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
