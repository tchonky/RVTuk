<Window x:Class="ReviTchucky.UI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ReviTchucky – Settings"
        Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Library Folder -->
        <TextBlock Grid.Row="0" Text="Family Library Folder:" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding LibraryFolderPath, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" Padding="4,2" Margin="0,0,6,0"/>
            <Button Grid.Column="1" Content="Browse…" Width="70"
                    Command="{Binding BrowseLibraryCommand}"/>
        </Grid>

        <!-- Derived DB path hint -->
        <TextBlock Grid.Row="2" Margin="0,0,0,12" Foreground="#888" FontSize="11"
                   TextTrimming="CharacterEllipsis">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DerivedDatabasePath}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
            <Run Text="Database: "/>
            <Run Text="{Binding DerivedDatabasePath, Mode=OneWay}"/>
        </TextBlock>

        <!-- Validation + Buttons -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding ValidationMessage}"
                       Foreground="Red" VerticalAlignment="Center" TextWrapping="Wrap"/>
            <Button Grid.Column="1" Content="Save" Width="70" Margin="6,0,0,0"
                    Command="{Binding SaveCommand}" IsDefault="True"/>
            <Button Grid.Column="2" Content="Cancel" Width="70" Margin="6,0,0,0"
                    Click="Cancel_Click"/>
        </Grid>
    </Grid>
</Window>
