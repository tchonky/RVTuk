<!-- ReviTchucky.UI/Views/InstructionsEditorWindow.xaml -->
<Window x:Class="ReviTchucky.UI.Views.InstructionsEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:ReviTchucky.UI.Controls"
        Title="Edit Family Info"
        Width="640" Height="560" MinWidth="480" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>  <!-- gallery -->
            <RowDefinition Height="Auto"/>  <!-- footer -->
        </Grid.RowDefinitions>

        <!-- HEADER: thumbnail + name + status -->
        <Grid Grid.Row="0" Margin="10,8,10,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Thumbnail with ⁝ menu button -->
            <Grid Grid.Column="0" Width="84" Height="68">
                <Border BorderThickness="2" CornerRadius="4">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#777"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CanUpdateOle}" Value="True">
                                    <Setter Property="BorderBrush" Value="OrangeRed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Image Source="{Binding ThumbnailSource}" Stretch="Uniform"
                           AllowDrop="True" x:Name="ThumbnailImage"/>
                </Border>
                <!-- Three-dot button -->
                <Button Content="&#x2056;" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Width="18" Height="18" FontSize="12" Padding="0" Margin="0,2,2,0"
                        Click="ThumbMenuButton_Click">
                    <Button.ContextMenu>
                        <ContextMenu x:Name="ThumbContextMenu">
                            <MenuItem Header="&#x1F5BC; Replace&#x2026;"                   Command="{Binding ReplaceThumbnailCommand}"/>
                            <MenuItem Header="&#x21BA; Reset to system original"    Command="{Binding ResetThumbnailCommand}"/>
                            <Separator/>
                            <MenuItem Header="&#x2191; Update .rfa with DB image"
                                      Command="{Binding UpdateOleCommand}"
                                      IsEnabled="{Binding CanUpdateOle}"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
            </Grid>

            <StackPanel Grid.Column="1" Margin="10,4,0,4" VerticalAlignment="Center">
                <TextBlock Text="{Binding FamilyDisplayName}" FontSize="13" FontWeight="Bold"/>
                <TextBlock Text="{Binding ThumbStatus}" FontSize="10" Foreground="Gray" Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>

        <!-- TOOLBAR -->
        <ToolBar Grid.Row="1">
            <Button Content="B" FontWeight="Bold"  Click="Bold_Click" Width="24"/>
            <Button Content="I" FontStyle="Italic" Click="Italic_Click" Width="24"/>
            <Button Click="Underline_Click" Width="24">
                <TextBlock Text="U" TextDecorations="Underline"/>
            </Button>
            <Separator/>
            <Button Content="H1" Click="H1_Click" Width="28"/>
            <Button Content="H2" Click="H2_Click" Width="28"/>
            <Button Content="&#x2261; List" Click="List_Click"/>
            <Separator/>
            <Button Content="&#x1F5BC; Add Image" Click="AddImage_Click"/>
        </ToolBar>

        <!-- EDITOR BODY -->
        <RichTextBox Grid.Row="2" x:Name="Editor" Margin="8" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     AllowDrop="True"
                     ctrl:RichTextBoxHelper.DocumentXaml="{Binding InstructionsXaml}"/>

        <!-- GALLERY -->
        <DockPanel Grid.Row="3" Margin="8,0,8,0" LastChildFill="False">
            <TextBlock Text="GALLERY" FontWeight="SemiBold" FontSize="11"
                       VerticalAlignment="Center" DockPanel.Dock="Left"/>
            <Button Content="Add to Gallery…" Command="{Binding AddImageCommand}"
                    DockPanel.Dock="Right"/>
        </DockPanel>
        <ScrollViewer Grid.Row="3" Margin="8,22,8,4" MaxHeight="150"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding GalleryItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="150" Margin="6">
                            <Image Source="{Binding Image}" Height="90" Stretch="Uniform"/>
                            <TextBox Text="{Binding Caption}" Margin="0,3,0,0"
                                     Tag="{Binding Id}" LostFocus="Caption_LostFocus"/>
                            <Button Content="Delete" Margin="0,3,0,0"
                                    Command="{Binding DataContext.DeleteImageCommand,
                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding Id}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- FOOTER -->
        <Grid Grid.Row="4" Margin="8,4,8,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="70" Margin="0,0,8,0"/>
                <Button Content="Save"   Click="Save_Click"                Width="70"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
